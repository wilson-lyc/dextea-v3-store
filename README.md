# dextea-store

Node.js 项目，采用前后端分离结构。

## 目录结构

```
dextea-store/
├── package.json        # 根项目配置（npm workspaces）
├── apps/
│   ├── api/            # 后端服务（暂时为空）
│   └── web/            # 前端服务（暂时为空）
└── README.md
```

## 使用

- 后端服务位于 `apps/api`
- 前端服务位于 `apps/web`

后续可在对应文件夹中初始化各自的服务。
